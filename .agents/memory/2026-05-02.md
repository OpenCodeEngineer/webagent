# 2026-05-02 — MVP Release Readiness Assessment (continued)

## E2E Widget Chat Test Results
- Widget embed (`widget.js`) loads correctly on third-party sites (tested on example.com)
- Widget requires `data-agent-token` + `data-user-id` attributes (NOT `data-agent-id`)
- Widget bubble renders, opens chat panel, accepts user messages
- WebSocket connects to `wss://dev.lamoom.com/ws` via proxy successfully
- **FAIL: Agent response timed out (120s)**
  - Gateway cold start took 116s for prep stages alone (core-plugin-tools: 22s, bundle-tools: 15s, system-prompt: 39s, stream-setup: 37s)
  - Agent run itself took 71s after prep
  - Total: ~187s, well over the 120s widget timeout
  - This is a **cold start** problem on the 2-vCPU VM — subsequent requests may be faster

## Database
- PostgreSQL on Neon (not SQLite as previously assumed)
- Connection: `ep-holy-paper-amjr6fh3.c-5.us-east-1.aws.neon.tech/neondb`
- Tables: users, accounts, sessions, verification_tokens, agents, customers, audit_log, meta_agent_messages, meta_agent_sessions, widget_embeds, widget_sessions
- 10 agents total, ~7 active (all belonging to dzianisvv@gmail.com customer)

## VM SSH Access
- Use `root@78.47.152.177` (not `openclaw@`)
- Gateway runs as user `openclaw` (systemd user service)
- Gateway logs: `su - openclaw -c 'journalctl --user -u openclaw-gateway ...'`

## Credentials Known
- `demo@lamoom.com` / `demo123` — works, has no active agents
- `dzianisvv@gmail.com` — Google OAuth only, no known password (credentials login fails with admin123)
- Proxy API token: `webagent-hooks-secret-prod-v1` (also exposed as NEXT_PUBLIC_)

## VibeBrowser Remote Relay
- UUID: `f6148397-8dca-4737-902f-089f2744fc9b`
- Relay URL: `wss://relay.api.vibebrowser.app/f6148397-8dca-4737-902f-089f2744fc9b`
- CLI: `npx -y --package @vibebrowser/mcp@latest vibebrowser-cli --remote f6148397-8dca-4737-902f-089f2744fc9b`
- Extension connected, 44 tools available
- Use for real browser automation (HubSpot signup, Google OAuth, etc.)
